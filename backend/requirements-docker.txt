# ──────────────────────────────────────────────────────────────
# SignBridge Pro — Docker-only runtime dependencies
# (Jupyter / notebook / training / dev tooling removed)
# ──────────────────────────────────────────────────────────────

# — Web framework ------------------------------------------------
fastapi==0.129.0
uvicorn[standard]==0.41.0
python-dotenv==1.2.1
python-multipart==0.0.22
websockets==16.0
pydantic==2.12.5

# — ML / Computer Vision ----------------------------------------
tensorflow==2.20.0
keras==3.13.2
mediapipe==0.10.32
opencv-python-headless==4.13.0.92
numpy==2.4.2
scipy==1.17.0
scikit-learn==1.8.0
h5py==3.15.1

# — Translation --------------------------------------------------
lingodotdev==1.4.1
httpx==0.28.1

# — TTS / Audio --------------------------------------------------
gTTS==2.5.4
sounddevice==0.5.5

# — Utilities ----------------------------------------------------
requests==2.32.5
tqdm==4.67.3
protobuf==6.33.5
