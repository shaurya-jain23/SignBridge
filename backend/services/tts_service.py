"""
SignBridge Pro — Text-to-Speech Service (Phase 0: Stub)

In Phase 3, this will use gTTS to generate audio from text.
"""


async def text_to_speech(text: str, lang: str = "en") -> bytes:
    """
    Convert text to speech audio.
    Phase 0: Returns empty bytes (stub).
    Phase 3: Will generate MP3 via gTTS.
    """
    return b""
